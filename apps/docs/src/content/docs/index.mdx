---
title: Getting Started
description: A comprehensive, modular SEO toolkit for TypeScript and React
---

# @power-seo

A comprehensive, modular SEO toolkit for TypeScript and React applications. Built with tree-shaking, type safety, and framework flexibility in mind.

## Features

- **Modular** — Install only the packages you need
- **Type-safe** — Full TypeScript support with strict types
- **Framework-agnostic** — Core utilities work anywhere, with React bindings available
- **SSR-optimized** — First-class support for Next.js, Remix, and generic SSR
- **Comprehensive** — From meta tags to structured data, sitemaps to analytics

## Quick Start

```bash
# Install core + meta for Next.js
npm install @power-seo/core @power-seo/meta

# Or install with React components
npm install @power-seo/core @power-seo/react @power-seo/schema
```

## Package Overview

| Package | Description |
| --- | --- |
| `@power-seo/core` | Core utilities, types, and constants |
| `@power-seo/react` | React SEO components (`<SEO>`, `<DefaultSEO>`) |
| `@power-seo/schema` | JSON-LD structured data builders + React components |
| `@power-seo/meta` | SSR meta tag helpers for Next.js, Remix, and generic SSR |
| `@power-seo/content-analysis` | Yoast-style content scoring engine |
| `@power-seo/preview` | SERP, Open Graph, and Twitter Card preview generators |
| `@power-seo/readability` | Flesch-Kincaid, Gunning Fog, Coleman-Liau, ARI algorithms |
| `@power-seo/sitemap` | XML sitemap generation, streaming, and validation |
| `@power-seo/redirects` | Redirect engine with URL pattern matching |
| `@power-seo/links` | Internal link graph analysis and suggestions |
| `@power-seo/audit` | SEO page and site auditing |
| `@power-seo/images` | Image SEO: alt text, lazy loading, image sitemaps |
| `@power-seo/ai` | LLM-agnostic prompt templates for AI-assisted SEO |
| `@power-seo/analytics` | Analytics data merging, trends, and dashboards |
| `@power-seo/search-console` | Google Search Console API client |
| `@power-seo/integrations` | Semrush and Ahrefs API clients |
| `@power-seo/tracking` | Analytics script builders with consent management |

## Next.js Example

```typescript
import { createMetadata } from '@power-seo/meta';

export const metadata = createMetadata({
  title: 'My Page',
  titleTemplate: '%s | My Site',
  description: 'A great page on my site',
  canonical: 'https://example.com/my-page',
  openGraph: {
    type: 'website',
    images: [{ url: 'https://example.com/og.jpg', width: 1200, height: 630 }],
  },
});
```

## Remix Example

```typescript
import { createMetaDescriptors } from '@power-seo/meta';

export const meta = () =>
  createMetaDescriptors({
    title: 'My Page',
    description: 'A great page on my site',
    canonical: 'https://example.com/my-page',
  });
```
